dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install python-networkx

test:
  override:
    - fab test:
        timeout: 1800

general:
    artifacts:
        - "htmlcov"

machine:
  services:
    - docker
