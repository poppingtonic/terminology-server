machine:
  services:
    - docker

dependencies:
  override:
      - docker info
      - docker build -t slade/terminologyserver .:
          timeout: 2700

test:
  override:
    - docker run -t -i fab -f /opt/slade360-terminology-server/fabfile.py test:
        timeout: 2700

general:
    artifacts:
        - "htmlcov"
