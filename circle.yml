machine:
  services:
    - docker

dependencies:
  override:
      - docker info

test:
  timeout: 2700
  override:
    - docker build -t slade/terminologyserver .
    - docker run -t -i -v .:/opt/src slade/terminologyserver fab -f /opt/src/fabfile.py test

general:
    artifacts:
        - "htmlcov"
