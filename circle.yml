machine:
  services:
    - docker

dependencies:
  override:
      - docker info

test:
  override:
    - docker build -e CIRCLECI=true -t termserver .:
        timeout: 2700
    - docker tag termserver docker.slade360.co.ke/savannah/termserver:
        timeout: 30
    - docker push docker.slade360.co.ke/savannah/termserver:
        timeout: 600
